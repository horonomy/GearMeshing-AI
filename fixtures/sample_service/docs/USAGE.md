# sample_service usage notes (fixture)

This is a synthetic fixture package with no real business logic. See
`../NOTICE` for the licensing note and `../README.md` for how to run its
checks.

## Modules

- `inventory_service.catalog` — toy in-memory catalog lookups.
- `inventory_service.pricing` — toy pricing arithmetic.
- `inventory_service.api` — plain-function facade over the two modules
  above.
